[tools]
tuist = "4.195.11"
swift = "6.0"
# Spellcheck (crate-ci/typos) — `just check` runs it locally; CI installs it via Homebrew
# (`typos-cli`) so its `just check` runs the same gate.
typos = "1.48.0"
