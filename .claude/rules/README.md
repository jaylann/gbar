# Project rules

Drop domain-specific rule files here (e.g. `github-api.md`, `logging.md`). They load
automatically by file path for agents working in this repo. Keep each one terse and
specific to a single concern.
